apiVersion: v1
kind: Service
metadata:
  name: vault-color-demo
  namespace: vault-demo
  labels:
    app: vault-color-demo
spec:
  type: LoadBalancer
  selector:
    app: vault-color-demo
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
